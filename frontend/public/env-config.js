// Dev-mode placeholder. In production this file is regenerated at container
// startup from the frontend's own .env file (see docker-entrypoint.sh) — do
// not hardcode real values here.
window.__ENV__ = {
  API_URL: '',
};
